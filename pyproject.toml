[project]
name = "advent-of-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "tqdm>=4.67.1",
    "typer>=0.14.0",
]

[project.scripts]
aoc = "aoc.cli:main"

[options.packages.find]
where = "src"
